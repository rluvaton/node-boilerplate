// Should not import anything

// Not using docker-compose.yml to avoid any possible issues with dealing with actual database
export const TEST_DOCKER_COMPOSE_FILE_NAME = 'docker-compose-test.yml'

export const POSTGRES_TEST_PORT = 15432
